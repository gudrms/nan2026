# AGENTS.md — 윷놀이: 까치호랑이 한판 작업 규칙

이 문서는 NHN 게임 AI 사전 과제 프로젝트를 개발하는 AI 에이전트의 작업 규칙이다.
**새 세션은 이 문서부터 읽는다.** 모든 작업 전 이 문서와 아래 문서 지도를 기준으로 판단한다.

### 문서 지도 (무엇을 볼지 / 어디에 쓸지)

| 문서 | 답하는 질문 | 읽기 | 쓰기(갱신 트리거) |
| --- | --- | --- | --- |
| **`AGENTS.md`** (이 문서) | 어떻게 작업하나 (규칙) | 항상 먼저 | 규칙 변경 시 (변경은 ADR 또는 커밋 메시지로 추적) |
| **`docs/Requirements.md`** | 무엇을 제출해야 하나 (과제 공고 원문) | 제출물·요건 판단 시 | ❌ **외부 원문 — 수정 금지** |
| **`docs/ADR.md`** | **왜 이렇게 결정했나** (결정 로그) | 결정 배경 확인 시 | 결정이 새로 생기거나 바뀔 때 (하단에 `ADR-XXX` 추가) |
| **`docs/CONCEPT.md`** | 무슨 게임인가 (기획 SoT: 룰 스코프·캐릭터·화면·데모 시나리오). **LLM 페르소나 프롬프트의 원본** | 기획·디자인·프롬프트 작업 전 | 기획 변경 시 — **코드보다 이 문서를 먼저 고친다** |
| **`docs/spec.md`** | 어떻게 만드나 (기술 SoT: 스택·디렉터리·API 계약·마일스톤) | 구현 전 | 구조·계약·스택 변경 시 |
| **`docs/AI_USAGE.md`** | AI를 어떻게 활용했나 (**제출물 4의 원고**) | 세션 시작 시 훑기 | **매 세션 실시간 추가** — 마감 직전 재구성 금지 |
| **`docs/checklist.md`** | 단계별 진행 상태(체크박스) | 다음 할 일 확인 | 작업 완료 시 해당 항목 `[x]` + 한 줄 근거만 — **날짜별 진행블록/표 금지**. 이력=git log |

> SoT 원칙(중복 금지): 제출 요건=Requirements, 결정 이유·전후=ADR, 기획 내용=CONCEPT, 기술 구현값=spec, AI 활용 과정=AI_USAGE, 진행 시점=checklist. 같은 내용을 여러 문서에 복붙하지 않고 참조로 잇는다.

문서 간 충돌 시 우선순위: **사용자 지시 > Requirements(제출 요건) > AGENTS.md > spec > CONCEPT**.
단, 기획 내용(룰 스코프·캐릭터·연출)은 CONCEPT가, 기술 구현값은 spec이 각자 SoT다.

---

## 0. 한 줄 요약

LLM 대사 + 캐릭터별 TTS로 말하는 AI 3인과 겨루는 **2v2 윷놀이 웹 게임**을 AI 페어코딩으로 만들고,
**만드는 과정 자체(커밋 기록·AI 활용 로그)를 제출물로 축적**한다. 마감 후 변경 불가 — 과정 기록은 실시간이 원칙.

---

## 1. 팀 구성 (2인 — ADR-002)

| 이름 | 역할 | 담당 영역 |
| --- | --- | --- |
| 지형근 | 개발 | 설계·구현·테스트·배포, AI 페어코딩 주도 |
| 황인섭 | 기획 | 게임 컨셉·캐릭터·세계관, 룰/밸런스 방향, 대사 톤 검수 |

- 제출물 5 **팀원 롤 기술서(PDF) 제출 필요.** 분업 내역은 개발 중 기록해 둔다 (마감 직전 재구성 금지)

---

## 2. 절대 규칙 (해서는 안 되는 것)

1. **API 키를 클라이언트·저장소에 노출하지 않는다.** `OPENAI_API_KEY`는 Vercel 환경변수(서버 전용)만. 프록시(`api/`) 밖에서 키를 참조하는 코드를 만들지 않는다. 공개 저장소이므로 한 번의 실수가 즉시 유출이다 (ADR-001 D6).
2. **LLM/TTS가 게임 진행을 블로킹하지 않는다.** 말 이동 판단은 로컬 알고리즘, 대사·음성은 비동기 + 3초 타임아웃 + 프리셋 폴백. 네트워크가 끊겨도 한 판이 완주 가능해야 한다 (ADR-001 D4).
3. **타인 저작물 에셋을 들이지 않는다.** 허용은 ① 팀 자체 제작 이미지(캐릭터 — ADR-003) ② CC0 효과음 ③ 라이선스 확인된 무료 폰트뿐이고, 그 외 시각·음향 요소는 전량 SVG/CSS/Web Audio 코드 생성. 에셋 도입 즉시 `AI_USAGE.md` 출처 표에 제작 방식·도구를 기록한다 (미기록 시 선발 취소 사유).
4. **`src/game/`은 순수 TS로 유지한다.** React/DOM/네트워크 무의존, import 방향은 바깥→안 단방향. 게임 코어가 헤드리스 시뮬레이션 가능해야 한다는 전제(spec §4.5)를 깨지 않는다.
5. **스코프를 임의로 넓히지 않는다.** MVP 범위는 CONCEPT §11, 후순위(빽도·STT·배경음·난이도)는 M4 이후에만 판단. 데모 영상 시나리오(CONCEPT §10)가 연출 가능한지가 우선순위 기준이다.
6. **커밋 히스토리를 훼손하지 않는다.** 커밋 기록 유지가 제출 요건이다. force push·히스토리 rewrite 금지. 기능 단위 커밋, 뭉텅이 커밋 금지.
7. **타인 저작물을 가져오지 않는다.** DiceForest는 아이디어 참고만 — 코드·에셋 복사 금지 (ADR-001 D1). 오픈소스 사용 시 출처·라이선스를 `AI_USAGE.md`에 기록.
8. **확인되지 않은 사실을 단정하지 않는다.** 룰 판정·확률·승률은 테스트와 시뮬레이션으로 실측한다. 불확실하면 "추정"임을 명시한다.

---

## 3. 작업 순서 (마일스톤 기준, 테스트 우선)

마일스톤과 완료 기준(verify)은 **spec §9**가 SoT다: M1 게임 코어 → M2 화면 → M3 LLM/TTS → M4 연출 → M5 배포·제출물.

- **작업 단위는 `docs/checklist.md` 항목 기준.** 무엇을 할지 해당 항목으로 먼저 식별하고, 끝나면 그 항목의 상태만 갱신한다 (`[x]` + 한 줄 근거).
- **게임 코어는 테스트와 함께 커밋한다.** `rules/state/botAI`의 룰 변경·추가는 해당 Vitest 테스트 없이 커밋하지 않는다. 윷 던지기는 고정 시드로 재현 가능하게.
- **봇 개선은 승률로 입증한다.** 판단 AI를 바꾸면 시뮬레이션 하네스(봇 vs 봇 N판)로 전후 승률을 비교하고, 결과를 커밋 본문과 `AI_USAGE.md`에 남긴다.
- **UI/연출은 데모 시나리오로 점검한다.** CONCEPT §10의 30~60초 시나리오가 실제로 촬영 가능한지를 수동 확인 기준으로 삼는다.

---

## 4. 커밋 규칙 (커밋 기록 = 제출물)

- **작업단위(구현+테스트+문서 동기화)가 끝나면 커밋한다.** 진행 방향(무엇을 할지·설계)은 사용자 확인을 받되, 승인된 작업단위의 결과물 커밋은 다시 묻지 않는다. **푸시는 매번 확인** (push → Vercel 자동 배포이므로).
- **메시지: type prefix만 영문, 나머지(제목 요약+본문)는 한글.** 제목 = `feat:`/`fix:`/`docs:`/`test:`/`chore:`/`refactor:` prefix + **한글 요약**. 본문 = 한글로 무엇을·왜, **AI 활용 맥락·정량 근거 표기** — 커밋 로그가 제출물 4의 증거 자료다.
  - 예: `feat: 기대값 탐색 봇 구현` / 본문 "휴리스틱 봇 대비 승률 +N%p (1000판 시뮬레이션). AI 제안 중 탐색 깊이 3은 기각(속도)."
- 파괴적 git 명령(force push, reset --hard 등)은 실행 전 항상 확인.

---

## 5. AI 활용 기록 규칙 (제출물 4 직결)

- **세션마다 `AI_USAGE.md`에 추가한다**: ① 주요 프롬프트와 결과 ② AI 제안의 **채택/기각과 이유** ③ 정량 근거(테스트·시뮬레이션).
- **기각 사례가 핵심 증거다.** "AI가 시킨 대로 다 했다"가 아니라 "AI 제안을 검증하고 기각/교정한 사례"를 우선 기록한다.
- 인게임 프롬프트(페르소나·대사 생성)의 설계·튜닝 과정도 여기에 기록한다 — 제출물 4의 "AI 대상 주요 프롬프트" 요건.

---

## 6. 문서 동기화

- **결정이 생기거나 바뀌면** → `docs/ADR.md` 하단에 `ADR-XXX` 추가 (상태·전후·이유). 다른 문서는 이유를 복붙하지 않고 ADR 번호로 참조한다.
- **기획이 바뀌면** → `CONCEPT.md`를 코드보다 먼저 갱신 (문서 말미 원칙). 페르소나 변경은 프롬프트(`personas.ts`/`api/dialogue.ts`)와 동기화.
- **구조·API 계약·스택이 바뀌면** → `spec.md` 갱신.

---

## 7. 알려진 함정 / 제약

- **브라우저 자동재생 정책**: 첫 사용자 클릭 전 오디오 재생 불가 → AudioContext는 첫 클릭 후 활성화 (spec §4.4).
- **Vercel 무료 티어**: Functions 실행 시간·호출량 제한. 대사 트리거는 이벤트 순간만(매 턴 아님), 프록시에 레이트 리밋.
- **모바일 확인**: iOS Safari·Android Chrome에서 터치 조작·레이아웃 확인 없이 M4를 닫지 않는다.
- **제출 링크 유지**: GitHub·YouTube·배포 URL은 심사 종료까지 접근 가능 상태 유지. 접수 마감 후 제출 내용 변경 불가.
- **영상 요건**: 플레이 영상은 실제 플레이 화면 그대로 — AI 합성·편집 조작 불가 (Requirements).
