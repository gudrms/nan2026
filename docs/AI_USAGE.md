# AI 활용 로그 (AI_USAGE)

> **제출물 4 "AI 활용 기술 문서"의 원고.** 개발 과정에서 AI를 어떻게 활용했는지 세션 단위로 실시간 기록한다.
> 기록 원칙: ① 주요 프롬프트(요청)와 결과 ② AI 제안의 **채택/기각과 그 이유** ③ 정량 근거(테스트·시뮬레이션)가 있으면 함께.

## 사용 AI 도구

| 도구 | 용도 | 비고 |
|---|---|---|
| Claude Code (Opus 4.8 / Sonnet 5) | 기획 토론, 문서 작성, 설계, 페어코딩, 테스트 생성 | 개발 전 과정의 주 도구 |
| OpenAI Codex 이미지 생성 | 캐릭터 emotion별 PNG 생성·기준 이미지 스타일 전이 | joy/anger/surprise 12종 |
| OpenAI Chat Completions | (인게임) 캐릭터 대사 실시간 생성 | 서버리스 프록시 경유 |
| OpenAI TTS | (인게임) 캐릭터 음성 출력 | 캐릭터별 보이스 3종 |

## 외부 에셋 / 오픈소스

| 항목 | 출처 | 라이선스 |
|---|---|---|
| (캐릭터 외 시각 요소 전량 SVG/CSS 코드 생성) | — | — |
| 캐릭터 이미지 21종 (4인 idle PNG·talk GIF + 단체 일러스트 + emotion PNG 12종, 범이는 talk GIF를 10프레임 512×512 애니메이션으로 재교체) | 팀 자체 제작 — 기획(황인섭)이 **OpenAI Codex**(이미지 생성)로 제작·전달. emotion 12종은 `group.png`를 스타일 기준으로 joy/anger/surprise 단체 시트를 각각 생성한 뒤 투명 배경·1254×1254 개별 PNG로 분리 (ADR-003) | 자체 저작물(AI 생성) |
| Pretendard 폰트 (본문·대사) | https://github.com/orioncactus/pretendard | SIL OFL 1.1 |
| Gugi 폰트 (디스플레이·타이틀) | Google Fonts | SIL OFL 1.1 |
| *추가 시 여기에 기록* | | |

---

## 세션 로그

### S1. 2026-07-24 — 과제 분석·컨셉·설계 (Claude Code)

> 결정의 상세 근거는 [ADR.md](./ADR.md)·[spec.md](./spec.md)에 있다. 여기는 **AI를 어떻게 활용했는지의 과정**만 기록한다.

**AI에게 시킨 일과 진행 방식**
- 과제 공고문 원문을 주고 **출제 의도 분석** 의뢰 → "결과물이 아닌 AI 활용 개발 과정 평가"라는 해석 확보. 기존 Unity 프로젝트(DiceForest)의 제출 리스크(커밋 기록 부재, 팀 산출물 저작권)를 AI가 저장소 스캔으로 진단 → 신규 제작 결정의 근거가 됨 (→ ADR-001)
- 플랫폼·언어·프레임워크를 **AI와 문답으로 좁혀감**: Unity vs 웹 / Node vs Python / Next.js 여부 / React 여부. 각 선택지의 트레이드오프를 AI가 제시하고 개발자가 결정 (→ ADR, spec §1)
- 게임 컨셉 2안(주사위 레이스 vs 윷놀이)을 AI에 비교 평가시킴 → 윷놀이 2v2 채택, 까치호랑이 민화 모티프의 캐릭터·세계관을 AI와 공동 설계 (→ CONCEPT.md)
- 기존 저장소 에셋의 **라이선스를 AI가 전수 조사** (license.txt 확인 포함) → CC0 1건 외 전부 출처 불명 판정 → 외부 에셋 0개 방침 확정

**AI 제안을 기각한 사례** (검증 역량의 증거)
- "봇 이동 판단을 LLM으로" 안 → 기각: 지연·실패가 게임 진행을 블로킹. 판단은 로컬 알고리즘, LLM은 대사 생성만 (2-레이어 분리)
- MCTS/강화학습 제안 → 기각: 사전 과제 스코프 과잉, 마감 리스크

**개발자가 AI 제안을 검증·교정한 사례**
- 개발자: "윷은 1/5 균등 확률이면 되지 않나?" → AI가 이항분포 모델로 반증 (균등 시 추가 턴 40%로 템포 붕괴) → 검증 후 이항분포 채택 (spec §4.3)
- 개발자: "스택이 너무 무난하지 않나?" → AI가 "라이브러리 추가는 역효과, 알고리즘 깊이로 승부" 제안 → 기대값 탐색·시뮬레이션 하네스·대화 메모리·Structured Output 4종 채택 (spec §4)

**산출물**: ADR.md, CONCEPT.md, spec.md, 본 문서

---

### S2. 2026-07-25 — 작업 규칙 수립·팀 구성 확정 (Claude Code)

**AI에게 시킨 일과 진행 방식**
- AI에게 docs 전체를 학습시켜 현재 상태를 요약하게 함 → 이 과정에서 **문서 간 구성원 불일치를 AI가 발견** (CONCEPT: 2인 vs ADR-001: 1인). 확인 후 2인 팀으로 확정 → ADR-002 기록, 제출물 5(팀원 롤 기술서) 제출 대상으로 전환
- 타 프로젝트에서 운영하던 AGENTS.md 하네스(문서 지도·SoT 원칙·절대 규칙·커밋 규칙 구조)를 참조로 제공 → AI가 본 과제 성격(커밋 기록=제출물, AI 활용 로그 실시간 기록, API 키 보호)에 맞게 이식해 `AGENTS.md` 작성. 초안을 개발자가 검토·수정 후 확정 (커밋 제목 영어/본문 한글 규칙 등)
- spec §9 마일스톤을 작업 단위로 분해한 개발 체크리스트(`docs/checklist.md`) 작성 의뢰

**산출물**: AGENTS.md, ADR-002, docs/checklist.md, 본 섹션

---

### S3. 2026-07-25 — 게임 코어 구현 M0~M1 (Claude Code)

**AI에게 시킨 일과 진행 방식**
- 스캐폴딩(Vite+React+TS+Vitest)과 게임 코어 7모듈(board/throwYut/state/rules/events/botAI/simulate) + 테스트 44개를 AI 페어코딩으로 구현. "트레이드오프는 보류 목록에 쌓고 기본값으로 계속 진행" 방식으로 개발자 개입 없이 연속 진행 → 보류 결정 7건을 checklist에 목록화(D-1~D-7)해 나중에 일괄 확정
- 방(22) 경유 지름길 분기(5쪽 대각선에서 정지 시 최단 출구 전환), 업기 그룹 이동, 잡기 추가 턴 등 엣지케이스를 테스트 먼저 작성해 고정
- 시뮬레이션 하네스로 실측: 1000판 232ms, 승률 blue 50.7 : orange 49.3(선공 이점 미미), 던지기 분포가 이항분포 이론치와 일치(do 15.4/gae 34.7/geol 34.2/yut 13.1/mo 2.6%), 말 개수별 게임 길이(4개 53회 / 3개 39회 / 2개 26회 던지기) — 밸런싱 결정의 근거 데이터

**테스트가 잡아낸 사례**
- `dangerAt`(피격 위험 확률) 테스트 실패 → 원인은 구현 버그가 아니라 테스트 설계(대기 말의 투입 사정권 합산 누락). 위험도 모델이 "대기 말도 1~5칸을 위협한다"를 반영하고 있음을 역으로 확인

**설계 판단 (AI 제안 채택)**
- @types/node 전역 추가 대신 simulate.ts 내 최소 `declare const process` 선언 — 브라우저 번들 코드에 Node 전역 타입 누출 방지
- 게임 진행 순서(디자인 시안 → 코어)를 뒤집어 코어 우선: M0·M1은 디자인 무의존, 디자인은 기획자가 별도 진행

**봇 개선 정량 검증 (v1 휴리스틱 → v2 기대값 탐색)**
- v2: 수 적용 후 (내 진행 이득 + 상대 손실 + 추가 턴 기대 전진량) − (상대 던지기 확률분포를 펼친 최선 반격 잡기의 기대 손실). 캐릭터 성향 가중치(범발톱 공격형 / 꼬리아홉 안전형 / 깍이 균형) 내장
- 매치업 검증: v2 vs v1 — **선공 시 54.8%, 후공 시에도 54.3%** (각 2000판). 선공 이점(v1끼리 +0.7%p)을 뒤집는 우위로 개선 입증

**산출물**: src/game/ 7모듈, tests/ 5파일(47 테스트), 시뮬레이션 통계

---

### S4. 2026-07-25 — M2 화면 구현: 디자인 시스템 이식 (Claude Code)

**AI에게 시킨 일과 진행 방식**
- 기획이 클로드 디자인으로 만든 디자인 시스템(.dc.html)을 AI가 코드 검토 후 채택 판정 — 표정 4상태가 API emotion enum과 1:1, 윷판이 정확히 29노드임을 검증. 시안의 SVG(React.createElement)를 `Character.tsx` JSX로 직결 이식, 컬러·타이포는 CSS 변수 토큰으로 추출
- `useGame` 훅(봇 자동 진행 + 이벤트→프리셋 말풍선), 윷판 SVG, 화면 3종 구현 → **브라우저 자동 플레이 스크립트로 E2E 검증** (시작→한 판 완주→결과→재시작)
- 프리셋 대사 풀(`presetLines.ts`) 작성 — CONCEPT §4 말투 사양 반영, M3부터 LLM 폴백으로 승격

**실플레이 검증에서 발견·수정한 사례**
- LEAD_CHANGE(역전) 대사가 초반에 매 이동마다 남발 → 새 선두 진행도 문턱(≥10) 도입, "게임 후반 긴장감 연출"(CONCEPT §6.2) 의도 복원. 회귀 테스트 추가
- 조사 처리 오류("깍이이(가)") 등 UI 문구 수정

**설계 판단**
- 게임판을 spec의 Canvas 대신 **SVG로 구현** (노드 클릭 이벤트·CSS transition 이동 애니메이션·SVG 아트 방향과 일관) — 보류 결정 D-8로 기록, 확정 시 spec 갱신
- 개발·데모용 `?mals=N` URL 파라미터 — E2E 테스트와 데모 템포 튜닝 겸용

**산출물**: src/components/ 7종 + screens 3종, useGame 훅, presetLines, 디자인 토큰

---

### S5. 2026-07-25 — M3 인게임 LLM 대사 + TTS 파이프라인 (Claude Code)

**AI에게 시킨 일과 진행 방식**
- 서버리스 프록시 2종 구현: `api/dialogue.ts`(페르소나 프롬프트 서버 상수 + Structured Output으로 text·emotion 강제 + 대화 메모리), `api/tts.ts`(캐릭터별 보이스 nova/onyx/shimmer + 톤 instructions). 공통: 입력 검증·IP 레이트 리밋(30/분)
- **인게임 페르소나 프롬프트 설계** (제출물 4의 "AI 대상 주요 프롬프트"): CONCEPT §4 캐릭터 사양 → 시스템 프롬프트 3종. 공통 규칙(한 문장 40자, 티키타카 지시) + 판세 요약(`situation.ts`)을 상황 컨텍스트로 주입
- 클라이언트: 3초 타임아웃 폴백(`dialogueClient`), TTS 순차 재생 큐·2개 초과 드롭·음소거(`ttsClient`). useGame에 이벤트→대사·표정·음성 동시 파이프라인 통합
- 검증: 폴백 경로 유닛 테스트 5종(네트워크 실패/503/빈 응답/emotion 오염) + **키 없는 로컬에서 한 판 완주 E2E** — "LLM이 죽어도 게임은 돈다"(ADR-001 D4)를 실측으로 입증

**남은 검증** (실제 API 키 필요 — Vercel 배포 후)
- 페르소나 말투 구분·티키타카 품질 튜닝, TTS 보이스 적합성, 실환경 타임아웃 폴백

**산출물**: api/ 2종, src/ai/ 3종(dialogueClient·ttsClient·situation), useGame 파이프라인, 테스트 5종

---

### S6. 2026-07-25 — 실배포 프롬프트 검증·훈수(HINT)·M4 연출 (Claude Code)

**실배포 API로 페르소나 프롬프트 검증 (제출물 4 핵심 소재 — 과금 절약 위해 3회만 호출)**
- 대표 케이스 3건(범발톱 도발 / 깍이 반격 티키타카 / 꼬리아홉 견제)을 배포 프록시에 호출 → 말투 캐릭터성은 확보됐으나 **문제 3건 발견**:
  ① 깍이가 시제 오해("이미 잡힌" 상황에 "잡히면 안 돼요") ② 꼬리아홉이 판세 숫자를 그대로 읊음("15와 12라니") ③ 40자 길이 제한 무시
- → 프롬프트 보강: "상황은 이미 일어난 일" 명시, 진행도 숫자 낭독 금지, emotion-감정 일치 규칙, 길이 규칙 완화·명료화(최대 두 문장 40자)

**깍이 훈수(HINT) 구현 — 판단 AI × 성격 AI 결합 지점**
- 플레이어 5초 장고 + 선택지 2개 이상일 때, 기대값 탐색이 계산한 최선 수의 유형(잡기/완주/업기/지름길/전진)을 상황 요약에 담아 깍이 대사로 생성 (폴백 프리셋 5종 병행). 브라우저 실동작 확인

**M4 연출**
- 효과음 전량 Web Audio 합성(외부 오디오 에셋 0개 유지): 윷 던지기(대역필터 노이즈 나무 클릭 4연타), 잡기(하강 임팩트), 완주(아르페지오) 등 6종. 음소거 토글 연동
- 캐릭터 SVG 반응형 전환 → 모바일 375px 가로 오버플로 없음 확인

**기획 실플레이 피드백 반영** (배포판 플레이 후 3건)
- "이벤트·음성과 말풍선 싱크가 안 맞는다" → 말풍선 수명을 고정 3.6초에서 **해당 대사의 TTS 재생 종료 시점 동기화**로 재설계 (speak()가 재생 종료에 resolve하는 구조로 ttsClient 개편)
- "화면이 펼쳐져 잘 안 보인다" → 윷 결과를 판 위 오버레이로 이동, 폭·간격 축소
- "말 이동 시 팀 색이 안 보인다" → 말 확대 + 팀색 후광 + 이동 그림자

**산출물**: 프롬프트 v2(api/dialogue.ts), HINT 파이프라인, src/audio/sfx.ts, 말풍선-음성 싱크, 모바일 보정

---

### S7. 2026-07-25 — 연출 개선 2차: 기획 실플레이 피드백 8건 (Claude Code)

**진행 방식** — 기획이 배포판을 플레이하고 피드백 8건 전달 → AI가 항목별 타당성·비용 평가표를 만들어 우선순위 합의 → 4개 작업단위로 나눠 구현·검증·커밋 (전 과정 로컬 폴백 모드로 API 과금 0)
- F-3·4: 좌우 팀 배치 (CONCEPT §7.2 원안 복원) / F-5·6: 가락 토스 연출·봇 템포·턴 바운스 / F-7·8: 등급별 결과음·경로 스텝 이동(코어의 move.path 설계가 그대로 활용됨) / F-1·2: 훈수 추천 칸 배지·플레이어 표정·음성 동기 타자기 말풍선

**AI가 검증 중 발견·해결한 문제 2건**
- 로컬 vite dev의 /api 404 응답이 타임아웃까지 매달려 폴백이 3초 지연 → dev는 프리셋 폴백 모드로 즉시 동작하게 분기 (spec §5의 "Vite dev + 프리셋 폴백 모드" 명문화)
- 숨긴 탭의 타이머 스로틀(1초)로 타자기가 느려짐 → 틱 증가 대신 경과시간 기반 계산으로 전환 (백그라운드 전환에도 강건)

**산출물**: 연출 개선 커밋 4건, 체크리스트 M4-2 전체 완료

---

### S8. 2026-07-25 — 연출 개선 3차: 음성 싱크 재설계·사회자 음성 (Claude Code)

- 기획 피드백 "AI끼리 연속 대화 시 말풍선이 음성보다 먼저 뜬다" → 근본 재설계: 말풍선을 **음성 재생이 실제 시작되는 순간** 표시 (TTS 재생 큐의 onStart 콜백 기반). 두 번째 화자의 말풍선은 자기 음성 차례까지 대기. 음성 없으면 즉시 표시, 큐 지연 4초 상한
- 사회자 음성: 캐릭터 TTS(과금)와 분리해 **브라우저 내장 Web Speech API(무료)** 채택 — 턴 안내("범발톱 차례")와 도개걸윷모 콜("걸!"). 비용 0으로 요구 충족한 대안 선택이 포인트
- 턴 시인성: ▼ 마커 + 이름표 강조 + 비활성 흐림 3중 표시

---

### S9. 2026-07-25 — 동기식 턴 진행 재설계 (Claude Code)

- 기획 피드백 "Web Speech 사회자 품질 미달·반복, 비동기라 부자연" → **사회자 폐기, 캐릭터 본인이 턴을 이끄는 동기식 턴 드라이버**로 재설계: 차례 알림("이 몸의 차례다!") → 던지기 → 결과 리액션("모다!! 크하하!") → 이동이 각 음성 종료를 기다리며 순차 진행. 플레이어 턴은 깍이가 안내·리액션 대행
- 비용 설계: 매 턴 대사가 늘어나는 만큼 턴 오프닝·결과 리액션은 **프리셋 풀**(캐릭터·등급별)로 하고 **TTS 세션 캐시**(같은 문장 1회만 합성)로 재과금 차단. 상황 의존 대사(잡기·역전 등)만 LLM 유지
- 15초 자동 플레이 실측으로 "오프닝→리액션→이벤트 반응" 시퀀스 확인 (YUT_MO 이벤트 대사는 결과 리액션과 중복이라 제거)

---

### S10. 2026-07-25 — 연출 5차: 대사량·BGM·잡기 임팩트 (Claude Code)

- "말이 너무 많고 느리다" → 이벤트당 1줄(잡기·종료만 2줄), 오프닝·리액션은 비대기 발화로 전환 — 음성 큐가 순서를 보장하므로 동기감은 유지하면서 턴 소요 절반 이하로
- 시작 인사 중 던지기 버튼 잠금(인사 음성 종료 시 개방), BGM(Web Audio 5음계 플럭 루프, 플레이어 차례에 패턴 전환 — 외부 음원 없이 합성), 이벤트 시 팀 4인 전체 표정 리액션, 잡기 3중 강화(붐 사운드+판 흔들림+버스트 링)
- **재피드백으로 롤백한 사례**: 비대기 발화(템포 개선안)가 배포판에서 음성 드리프트 재유발 → **발화 체인(speechTail)** 도입해 엄격 동기 복원 — 모든 대사(LLM 응답 대기 포함)가 끝나야 다음 액션. "빠르게"와 "동기"가 충돌할 때 동기를 우선하고 속도는 대사량 축소로 확보

---

### S11. 2026-07-30 — 음성 템포·README·중간 평가 리뷰 (Claude Code)

**AI가 개발자 지시를 교정한 사례**
- 기획 피드백 "음성이 늘어진다"에 개발자가 "0.5배속" 지시 → AI가 방향 모순 지적(0.5배속=더 느려짐), 1.25배속 제안·적용 후 실청취 재피드백으로 **1.5배속 확정**. `gpt-4o-mini-tts`는 speed 파라미터 미지원 → 클라이언트 `playbackRate` 채택, 말풍선 타자기 동기(재생 길이/배속)도 함께 보정

**README 작성 — 개발자가 AI 산출물을 교정한 사례**
- AI의 README 초안이 인게임 AI 시스템만 강조 → 개발자가 "과제는 AI 활용이 메인" 지적 → **AI 활용(개발 과정) 섹션 보강** (검증·기각 사례 프레임, 상세는 본 문서 참조로 연결 — SoT 원칙)

**AI를 요구사항 기반 평가관으로 세워 중간 코드리뷰·채점**
- 발견: ① 프록시 레이트 리밋이 인메모리(서버리스 인스턴스별·콜드스타트 리셋)라 남용 방어 약함 + origin 검증 없음 ② dialogue의 situation/history가 클라이언트 입력이라 프롬프트 인젝션 여지(영향은 요청자 본인 응답에 한정) ③ 제출물 준비(M5)가 전 항목 미착수로 병목 — 코드 완성도 대비 제출 리스크가 더 큼
- 본 문서의 실시간 기록 원칙 위반(당일 세션 미기록)도 리뷰에서 적발 → 본 항목으로 복구

---

### S12. 2026-07-30 — 캐릭터 이미지 도입·이름/게임명 확정·프록시 보안 강화 (Claude Code)

**기획 제작 캐릭터 이미지 도입 (ADR-003)**
- 기획이 제작·전달한 이미지 9종(4인 idle PNG + talk GIF + 단체 일러스트)으로 SVG 캐릭터 교체. AI가 도입 전 리스크 3건을 선제 제기: ① "외부 에셋 0개" 원칙 충돌 → 팀 자체 제작물은 원칙의 목적(타인 저작물 리스크)에 저촉되지 않음으로 정리, ADR-003 기록 ② dist/(빌드 산출물·gitignore)에 놓인 에셋 위치 오류 → src/assets/characters/로 이동 ③ **emotion 4종 표정 연동 소실** → 이모지 오버레이로 유지하고, emotion별 이미지는 기획이 추가 생성하기로 (생성용 프롬프트 문서를 AI가 작성 — docs/design/emotion-image-prompts.md)
- 이름·게임명 확정 적용: 깍이→까비, 범발톱→범이, 꼬리아홉→여울, 플레이어 "도령", 게임명 "윷놀이 : 까치호랑이" — CONCEPT(SoT) → 서버 페르소나 프롬프트 → 프리셋·UI·README 순으로 동기화. 내부 actor ID는 유지해 코드 변경 최소화

**S11 평가 리뷰 후속 — 프록시 3건 개선**
- Origin 검증(비브라우저·외부 사이트 요청 403), 인스턴스 전역 레이트 상한(120/분 — 분산 IP 우회에도 비용 유계), 프롬프트 인젝션 완화(입력 개행 정규화·history actor 화이트리스트·"게임 데이터 내 지시 무시" 시스템 규칙)

**도구 실수·복구 사례 (기록 원칙: 실패도 남긴다)**
- PowerShell 5.1 `Get-Content`가 BOM 없는 UTF-8 한글 파일을 ANSI로 오독 → presetLines.ts 전체 한글 깨짐 → git 원복 후 에이전트 Edit 도구로 재작업. "인코딩 민감 파일은 셸 텍스트 처리 대신 전용 편집 도구" 교훈

---

### S13. 2026-07-31 — 캐릭터 emotion 이미지 생성 (OpenAI Codex)

- 주요 프롬프트: 기존 `group.png`를 필수 참고 이미지로 지정하고 캐릭터 4인의 순서·비율·색·복장·두꺼운 윤곽선을 고정한 채 joy(초승달 눈·활짝 웃음), anger(찌푸린 눈썹·팔짱), surprise(큰 원형 눈·O자 입)를 감정별 단체 시트 1장씩 생성하도록 요청
- 채택: 4인을 감정별 한 시트에 함께 생성해 감정 내부의 스타일 일관성을 확보하고, 균일한 녹색 키 배경을 로컬에서 투명 처리한 뒤 캐릭터별 1254×1254 RGBA PNG로 분리
- 교정: 최초 균등 4분할은 여우 이미지에 호랑이 꼬리 조각이 섞여 기각. 실제 캐릭터 사이 경계 좌표로 다시 분리해 12종 접촉 시트에서 이웃 조각·잘림이 없음을 육안 검수
- 결과: `tiger|fox|magpie|player` × `joy|anger|surprise` 총 12종 생성. neutral은 기존 idle을 유지하며 코드 연동은 별도 작업으로 남김

---

### S14. 2026-07-31 — emotion 이미지 연동·이름 변경 누락분 정리·범이 GIF 교체 (Claude Code)

**emotion 이미지 12종 검수·연동**
- S13 산출물을 코드 반영 전 직접 검수: 12개 PNG 전부 idle과 동일 화풍·비율(육안 확인), PNG 헤더 검사로 RGBA 투명·1254×1254 규격 통일 확인 후 `Character.tsx`의 `ART`를 emotion 축으로 확장. talk GIF가 neutral 1종뿐이라 발화 중 emotion은 기존 이모지 배지로 계속 보완. StartScreen의 하드코딩된 `emotion="joy"` 캐릭터가 실제로 `tiger-joy.png`로 렌더링됨을 브라우저에서 확인해 검증 근거로 사용(별도 게임 플레이 없이도 emotion→이미지 매핑 실증 가능함을 활용)

**이름 변경 전수 재검사 (개발자 확인 요청 계기)**
- "이름까지 다 바뀐 거 맞나" 질문에 grep 전수 조사 → CONCEPT.md(§5·§6.2·§6.3·§7.2·§10)와 spec.md(API 예시·봇 가중치 설명)에 이전 이름 잔존분 발견, 정리. AI_USAGE·checklist는 과거 세션의 사실 기록이므로 의도적으로 미수정(AGENTS.md §5 원칙)임을 구분해 설명

**범이(호랑이) idle+talk GIF 교체 → talk 전용으로 재조정**
- 기획이 전달한 애니메이션 GIF(10프레임, 512×512, 카카오톡 전송분)를 처음엔 idle·talk 양쪽에 재사용 — 배포 직후 "기본 이미지 입이 계속 움직인다"는 피드백으로 문제 발견. 10프레임 전체를 추출해 검사한 결과 입이 벌어짐↔O자 사이만 반복하고 다문 입 정지 프레임이 없음을 확인 → **idle은 기존 tiger-idle.png(정지)로 원복, GIF는 talk 전용**으로 재조정. emotion 3종(PNG)은 변경 없음

**산출물**: Character.tsx(emotion 연동 + 범이 GIF), CONCEPT.md·spec.md·botAI.test.ts(이름 동기화), src/assets/characters/(범이 에셋 교체)

---

### S15. 2026-07-31 — 배포판 버그 리포트 2건 진단·수정 (Claude Code)

기획이 전달한 실플레이 이슈 2건을 코드 검토로 진단 (스크린샷·재현 요청 없이 근본 원인 특정)

**① 캐릭터 이름표 폰트 잘림** — 실측 진단 후 즉시 수정
- `.name-pill.active`에 `font-weight:700`이 걸려 있었는데 Gugi는 400 굵기만 제공하는 단일 웨이트 폰트 → 브라우저가 가짜 볼드 합성 → 받침 있는 한글 획이 뭉개짐. 브라우저에서 `getComputedStyle`로 실제 렌더링 굵기를 실측해 확진 후 font-weight 제거

**② "범이 차례에서 멈춘다"** — 로컬 재현 실패 → 코드 정적 분석으로 원인 특정
- 로컬 dev는 API 호출 자체를 건너뛰므로 브라우저 재현 불가 → `dialogueClient.ts`/`ttsClient.ts`의 fetch 타임아웃 로직을 정적 분석해 구조적 결함 발견: `clearTimeout(timer)`가 응답 **헤더** 수신 직후 호출되어, 그 뒤 `res.json()`/`res.blob()`로 **본문**을 읽는 구간은 타임아웃 보호가 없었음. 서버리스 콜드 스타트 등으로 본문 스트리밍이 멈추면 해당 await가 영구히 안 끝남
- useGame의 턴 드라이버는 대사 프로미스가 모두 끝나야 다음 액션이 진행되는 엄격 동기 구조(J-1)라, 대사 하나가 멎으면 게임 전체가 멈추는 구조임을 재확인. TURN_ORDER상 범이가 플레이어 다음 첫 봇 턴이라 세션 초반(콜드 스타트로 가장 불안정한 시점)의 호출이 몰리는 지점과 겹침 — "왜 범이 차례에서만"이라는 사용자 질문에 대해, 특정 캐릭터의 코드가 다른 게 아니라 **타이밍 의존 버그가 세션 초반 첫 봇 턴에 걸릴 확률이 가장 높다**는 설명으로 답변
- 수정: 타이머를 `finally`로 옮겨 본문을 다 읽을 때까지 유지 → abort 시 `res.json()`/`res.blob()`이 reject되어 정상 폴백. 회귀 테스트 추가(가짜 타이머로 "헤더는 오지만 본문이 멈추는" 상황 재현, 타임아웃 후 폴백 검증)

**교훈**: 로컬에서 재현 안 되는 버그라도 "폴백 경로가 실제로 모든 실패 모드를 커버하는가"를 코드로 추적하면 원인을 특정할 수 있다. `fetch` + `AbortController` 패턴에서 타이머를 헤더 수신 시점에 해제하는 실수는 흔하고 위험도가 높다(무기한 행).

**산출물**: dialogueClient.ts·ttsClient.ts(타임아웃 보호 확장), tests/dialogueClient.test.ts(회귀 테스트 1종), tokens.css(이름표 폰트)

---

### S16. 2026-07-31 — 조사 어법 누락분 추가 수정 (Claude Code)

- 배포판 자동 플레이 중 기획이 "윷다 크하하" 직접 목격·보고 → S15에서 고친 이에요/예요·라니/이라니 외에 **세 번째 조사 패턴**(받침 있는 음절에 "다"만 붙는 경우)을 놓쳤음을 확인. `da()` 헬퍼 추가해 범이 good 등급 대사 2곳 + YUT_MO 상대팀 견제 1곳 수정
- "가락이 잘못됐군!"이 무엇을 가리키는지 불명확하다는 지적 → "윷가락이 잘못됐군!"으로 명시
- 회귀 테스트에 다/이다 패턴과 YUT_MO 상대팀(orange) 분기 추가, 배포판에서 "윷이다!!" 렌더링을 자동 플레이 로그로 직접 확인

**산출물**: presetLines.ts(da 헬퍼), tests/presetLines.test.ts(확장)

---

### S17. 2026-07-31 — 전체 코드리뷰·평가관 채점 후 개선사항 반영 (Claude Code)

**AI를 평가관으로 세워 전체 코드베이스 재검토** (3,344줄 전수 확인, 이전에 안 읽었던 board.ts·simulate.ts·Board.tsx·TeamPanel.tsx·YutSticks.tsx·sfx.ts·bgm.ts·health.ts 포함) → 종합 81/100, 신규 발견 6건

**개발자 지시로 즉시 수정한 것** (제출 준비물은 기획 담당이라 범위 제외)
- 죽은 코드 제거: `api/health.ts`(M3에서 대체 예정이라던 더미, 참조 0건)
- 죽은 분기 정리: `useGame.ts`의 CAPTURE 무드 계산에서 도달 불가능한 `'surprise'` 중간값 대입 제거
- 문서 SoT 위반 수정: `spec.md`가 실제 구현(SVG `Board.tsx`, 서버 상수 페르소나)과 다르게 여러 세션째 Canvas/`personas.ts`/`BoardCanvas.tsx`를 서술하던 것을 실제 구조로 동기화
- 레이트리밋 Map 누수: `api/dialogue.ts`·`api/tts.ts`의 IP별 `RATE` Map이 유휴 키를 영구 보관하던 것을 매 호출 전체 정리로 수정
- 타입 안전성: 두 API 핸들러의 `req: any, res: any`를 실제 Vercel 런타임 형태를 반영한 최소 로컬 인터페이스로 교체 (`@vercel/node` 의존성 추가 없이)

**보류한 것**: UI/훅 레이어(`useGame.ts` 등) 테스트 커버리지 0%는 `@testing-library/react` 등 신규 의존성과 vitest 환경(jsdom) 변경이 필요해 범위가 다르다고 판단, 임의로 진행하지 않고 사용자 확인 대기

**산출물**: api/health.ts(삭제), useGame.ts·spec.md·api/dialogue.ts·api/tts.ts

---

### S18. 2026-08-06 — 기획 실플레이 버그 리포트 6건 진단·수정 + AI 리뷰어 3라운드 (Claude Code)

**기획 실플레이 피드백 6건**
① 승리 타이틀이 뜬 뒤에도 대사가 계속 나옴 ② 여울 차례일 때 캐릭터 이미지가 과도하게 커짐(까비는 적당히, 범이·도령은 안 커짐) ③ 여울 본인 차례에 던지고 이동한 뒤 "제 차례에요" 대사가 뒤늦게 또 나타남 ④ 여울이 완주시킨 뒤 "하지만 미리 생각해놓은거였나요?" 같은 의미불명 대사 ⑤ 범이 차례일 때 턴 표시 삼각형(▼)이 얼굴에 파묻힘 ⑥ 까비가 윷·모로 업고 한 번 더 할 때 "주깍?" 같은 의미불명 대사

**진단·수정 (코드 정적 분석 + 브라우저 자동 플레이 실측, 스크린샷·재현 요청 없이 근본 원인 특정)**
- **②**: idle PNG와 talk GIF의 캔버스 내 캐릭터 크롭 비율이 캐릭터마다 다름을 canvas 알파채널 바운딩박스로 실측해 확진(여울 idle 0.777 대 talk 0.918 — 최대 +23%, 범이는 0.784 대 0.785로 거의 동일). 에셋 재추출 없이 캐릭터별 역보정 계수(`TALK_SCALE`)를 CSS `transform: scale()`로 적용(발끝 기준 축소). 보정 후 오차 ±1.4% 이내로 축소
- **④·⑥**: `presetLines.ts`의 상대팀(범이·여울) 이벤트 대사 풀이 화자 구분 없이 `ev.actor`에 그대로 귀속되던 오배정을 발견 — 여울이 범이 말투("어흥!")로 말하는 등. 화자별 풀로 분리(`orangeSpeaker`)해 수정, 캐릭터 고유 어휘가 남의 대사에 섞였는지 전수 검사하는 회귀 테스트 추가. "주깍?"은 별개로 LLM이 감탄사 음절을 어미에 융합하는 현상(소형 모델+temperature 1.0)으로 진단 → `isWellFormedLine` 검증기 신설(문장부호·이모지·감탄사 융합 검사, 서버·클라이언트 이중 방어), 프롬프트에 감탄사 독립 토큰 지시 추가, temperature 0.8로 하향
- **①**: 원인 3개 중첩 — TTS 재생 큐가 모듈 전역이라 화면 언마운트 후에도 안 멈춤(`stopSpeech`+`epoch` 카운터로 정리), 완주(FINISH)와 게임종료(GAME_END)가 같은 수에서 겹쳐 대사 3줄 중복(FINISH를 GAME_END에 흡수), 결과 화면 전환이 고정 지연이라 마무리 대사를 못 기다림(`endingSpoken` 신호 + 상한 대기로 교체)
- **⑤**: `.turn-marker`가 캐릭터 이미지와 겹친 채 반대 방향(마커는 아래로, 캐릭터는 위로)으로 애니메이션되던 것을 확인. 마커 전용 밴드(`padding-top`)를 확보하고 애니메이션 방향·주기를 캐릭터 bob과 일치시킴
- **③**: 차례 안내 잠금(`turnOpenPending`/`openedTurnIdx`)이 드라이버가 안내 단계에 실제로 도달하기 전까지 열려 있어, 그 틈에 플레이어가 먼저 던지면 안내가 누락되거나 늦게 겹쳐 나오는 레이스를 발견. 턴이 바뀌는 즉시 잠그는 `turnAnnounced` 상태로 교체 — 뮤테이션 테스트로 원본 로직 재현 시 20초간 5ms 폴링 중 위반 3건, 수정 후 20,719샘플 중 0건 확인

**AI를 리뷰어로 세운 코드리뷰 — 1라운드** (병렬 에이전트 3종: 버그 스캔/CLAUDE.md 준수/비동기 상태 감사) — 위 6건 수정 직후 실시, 3건 추가 발견·즉시 수정
- `.char-bubble`이 마커 밴드(padding-top) 도입의 부작용으로 캐릭터 머리에서 16~20px 떠 보이는 CSS 회귀(수치 유도로 확진, mutation으로 20px 차이 재확인)
- `api/dialogue.ts`가 80자로 자른 뒤 검증하고 있어 정상 문장의 끝맺음 문장부호가 잘려 형식 오류로 오판될 수 있던 순서 버그 — 검증 후 자르기로 수정
- 승리 대사가 `waitSpeech()`(음성 종료 즉시 resolve)만 기다리다 자기 자신의 최소 노출 시간(`BUBBLE_MIN_MS`)을 못 채우고 결과 화면이 넘어갈 수 있던 문제 — `deliverLine`에 `waitForDisplay` 옵션을 추가해 GAME_END 대사만 말풍선이 실제로 사라질 때까지 대기(평소 턴 속도는 불변). 실측 전환 시간 6.15초가 `LINE_STAGGER_MS+BUBBLE_MIN_MS+RESULT_AFTER_SPEECH_MS` 계산과 일치함을 확인

**2라운드** (React 18 StrictMode 관점 재검토, 개발자 요청)
- 게임 시작 인사 이펙트가 cleanup 없이 `useEffect(..., [])`로만 작성돼, dev StrictMode의 마운트→언마운트→재마운트 시 유령 인스턴스가 살아남아 인사 대사가 두 번 겹치는 버그 발견. 기존 `disposed` 플래그는 이 과정이 같은 틱에서 동기적으로 정리돼 false로 되돌아와 못 걸러냄을 확인 — 이펙트 실행마다 독립된 `cancelled` 클로저로 잠그는 방식으로 수정. 임시 디버그 카운터로 실증(effect는 정말 2번 실행되지만 `deliverLine` 호출은 수정 전 2번 → 수정 후 1번), 검증 후 디버그 코드 제거

**3라운드** (병렬 에이전트 4종 + 직접 병렬 브라우저 테스트로 재검증)
- 에이전트: `cancelled`가 인스턴스별로 독립적인지, `disposed`와 충돌 없는지, 같은 취약 패턴(cleanup 없는 async 이펙트)이 다른 곳에도 있는지(턴 드라이버·훈수 타이머·BGM 이펙트 — 전부 ref 가드·멱등성으로 이미 안전함 확인) 등을 심층 재검증. 경미한 사항 2건(dev 전용 RNG 이중 소비, 클라이언트의 서버-재검증 중복) 확인 후 실제 플레이 영향 없다고 판단해 미수정
- 직접 병렬 브라우저 테스트: 데스크톱 탭(4.3분·6488샘플, 완주 사이클 2회+, 차례안내·화자오배정·정체 위반 0) + 모바일 375×812 탭(게임 중 음소거 152회 토글하며 3806샘플, 가로 오버플로·콘솔에러 0) 동시 실행

**커밋 구성**
- 작업 단위로 7개 커밋(화자오배정/TTS정리/마커위치/차례타이밍+승리전환/캐릭터확대/LLM검증/StrictMode수정)으로 분리. `useGame.ts`가 여러 관심사에 걸쳐 있어 훅 단위로 diff를 손으로 나누고, 각 중간 상태가 독립적으로 타입체크·테스트 통과하는지 확인하며 순서대로 커밋 후 전부 push

**산출물**: presetLines.ts·tests/presetLines.test.ts, ttsClient.ts·tests/ttsClient.test.ts(신규), tokens.css, useGame.ts·GameScreen.tsx, Character.tsx, dialogueValidate.ts(신규)·tests/dialogueValidate.test.ts(신규)·situation.ts·tests/situation.test.ts(신규)·api/dialogue.ts·dialogueClient.ts, 커밋 7건(전부 push). 최종 테스트 74개(10파일) 전부 통과

---

*다음: 재배포 후 이번 수정 실배포 환경에서 재확인, 실기기 모바일 확인, M5 배포·제출물(기획 담당)*
